{% set python_min = "3.11" %}
{% set name = "pyramids" %}
{% set version = "0.9.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/MAfarrag/pyramids/archive/{{ version }}.tar.gz
  sha256: dab72ca13f814b4e58db515038b638a5e3c65ba0c7c7acd270a41ce74095c3b4

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python {{ python_min }}
    - pip >=24.3.1
    - setuptools
  run:
    - python >={{ python_min }}
    - geopandas >=1.0.0
    - hpc-utils >=0.1.5
    - numpy >=2.0.0
    - pandas >=2.0.0
    - pyproj >=3.7.0
    - PyYAML >=6.0.0
    - Shapely >=2.0.0
    - gdal >=3.10.0
    - libgdal-netcdf >=3.10.0
    - libgdal-hdf4 >=3.10.0

  run_constrained:
    - cleopatra >=0.6.0


test:
  requires:
    - python {{ python_min }}
  imports:
    - pyramids

about:
  home: https://github.com/Serapieum-of-alex/pyramids
  license: GPL-3.0-only
  license_family: GPL
  license_file: LICENSE.md
  summary: GIS utility package
  description: |
    GIS utility package.
  dev_url: https://github.com/Serapieum-of-alex/pyramids
  doc_url: https://serapieum-of-alex.github.io/pyramids
  doc_source_url: https://github.com/Serapieum-of-alex/pyramids/main/README.md

extra:
  recipe-maintainers:
    - MAfarrag
